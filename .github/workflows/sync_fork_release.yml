name: Auto Sync Fork with Releases

on:
  workflow_dispatch:

env:
  UPSTREAM_REPO: "orion-lib/OrionTV"
  UPSTREAM_URL: "https://github.com/orion-lib/OrionTV.git"

jobs:
  sync-fork:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # 获取完整历史记录和所有标签[1](@ref)

      - name: Setup Git
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"

      - name: Add Upstream and Sync Tags
        run: |
          git remote add upstream ${{ env.UPSTREAM_URL }} || true
          # 强制同步所有标签，解决冲突[5](@ref)
          git fetch upstream --tags -f
          git push origin --tags -f

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y jq curl

      - name: Sync Releases and Assets
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "=== 开始同步 ${{ env.UPSTREAM_REPO }} 的 Releases 和 Assets ==="
          
          # 通过GitHub API获取所有上游Releases，确保完整性[5](@ref)
          UPSTREAM_RELEASES_URL="https://api.github.com/repos/${{ env.UPSTREAM_REPO }}/releases"
          PAGE=1
          ALL_TAGS=""
          
          while true; do
            RELEASES_PAGE=$(curl -s -H "Authorization: token $GITHUB_TOKEN" \
              "$UPSTREAM_RELEASES_URL?page=$PAGE&per_page=100")
            
            if [ "$(echo "$RELEASES_PAGE" | jq -r 'length')" -eq 0 ]; then
              break
            fi
            
            PAGE_TAGS=$(echo "$RELEASES_PAGE" | jq -r '.[] | select(.draft == false) | .tag_name')
            ALL_TAGS="$ALL_TAGS"$'\n'"$PAGE_TAGS"
            ((PAGE++))
          done
          
          # 清理并排序标签
          ALL_TAGS=$(echo "$ALL_TAGS" | grep -v '^$' | sort -V -r)
          
          echo "发现上游 Releases 标签:"
          echo "$ALL_TAGS"
          echo ""

          # 处理每个有Release的Tag
          for TAG in $ALL_TAGS; do
            echo "🔍 处理 Tag: $TAG"
            
            # 检查本地是否已有该Release
            LOCAL_RELEASE_URL="https://api.github.com/repos/$GITHUB_REPOSITORY/releases/tags/$TAG"
            LOCAL_RESPONSE=$(curl -s -H "Authorization: token $GITHUB_TOKEN" "$LOCAL_RELEASE_URL")
            LOCAL_RELEASE_ID=$(echo "$LOCAL_RESPONSE" | jq -r '.id // empty')
            
            if [ -n "$LOCAL_RELEASE_ID" ]; then
              echo "ℹ️ 本地已有 Release，跳过: $TAG"
              # 检查是否需要更新assets（如果上游有新增assets）
              UPSTREAM_RELEASE_URL="https://api.github.com/repos/${{ env.UPSTREAM_REPO }}/releases/tags/$TAG"
              UPSTREAM_RESPONSE=$(curl -s -H "Authorization: token $GITHUB_TOKEN" "$UPSTREAM_RELEASE_URL")
              UPSTREAM_ASSETS_COUNT=$(echo "$UPSTREAM_RESPONSE" | jq -r '.assets | length')
              LOCAL_ASSETS_COUNT=$(echo "$LOCAL_RESPONSE" | jq -r '.assets | length')
              
              if [ "$UPSTREAM_ASSETS_COUNT" -gt "$LOCAL_ASSETS_COUNT" ]; then
                echo "🔄 检测到上游有新增Assets，重新同步: $TAG"
                # 删除本地Release以便重新创建
                curl -s -X DELETE -H "Authorization: token $GITHUB_TOKEN" \
                  "https://api.github.com/repos/$GITHUB_REPOSITORY/releases/$LOCAL_RELEASE_ID"
              else
                echo ""
                continue
              fi
            fi
            
            # 获取上游Release详细信息
            UPSTREAM_RELEASE_URL="https://api.github.com/repos/${{ env.UPSTREAM_REPO }}/releases/tags/$TAG"
            UPSTREAM_RESPONSE=$(curl -s -H "Authorization: token $GITHUB_TOKEN" "$UPSTREAM_RELEASE_URL")
            UPSTREAM_RELEASE_ID=$(echo "$UPSTREAM_RESPONSE" | jq -r '.id // empty')
            
            if [ -z "$UPSTREAM_RELEASE_ID" ]; then
              echo "❌ 无法获取上游Release信息: $TAG"
              echo ""
              continue
            fi
            
            # 检查Assets数量
            ASSETS_COUNT=$(echo "$UPSTREAM_RESPONSE" | jq -r '.assets | length')
            if [ "$ASSETS_COUNT" -eq 0 ]; then
              echo "ℹ️ 上游Release没有Assets: $TAG"
              echo ""
              continue
            fi
            
            echo "✅ 发现 $ASSETS_COUNT 个Assets"
            
            # 下载Assets到临时目录
            echo "📥 下载Assets..."
            mkdir -p "temp_assets/$TAG"
            ASSET_URLS=$(echo "$UPSTREAM_RESPONSE" | jq -r '.assets[].browser_download_url')
            DOWNLOAD_SUCCESS=false
            
            for ASSET_URL in $ASSET_URLS; do
              if [ -n "$ASSET_URL" ]; then
                FILENAME="temp_assets/$TAG/$(basename "$ASSET_URL")"
                echo "⬇️ 下载: $(basename "$ASSET_URL")"
                if curl -L -f -o "$FILENAME" "$ASSET_URL"; then
                  echo "✅ 下载成功"
                  DOWNLOAD_SUCCESS=true
                else
                  echo "❌ 下载失败: $ASSET_URL"
                  rm -f "$FILENAME"
                fi
              fi
            done
            
            # 创建Release（只有成功下载了文件才创建）
            if [ "$DOWNLOAD_SUCCESS" = true ] && [ "$(ls -A temp_assets/$TAG/ 2>/dev/null)" ]; then
              echo "🚀 创建Release: $TAG"
              
              # 获取上游Release的详细信息用于创建本地Release
              UPSTREAM_NAME=$(echo "$UPSTREAM_RESPONSE" | jq -r '.name // empty')
              UPSTREAM_BODY=$(echo "$UPSTREAM_RESPONSE" | jq -r '.body // empty')
              UPSTREAM_PRERELEASE=$(echo "$UPSTREAM_RESPONSE" | jq -r '.prerelease // false')
              
              # 设置Release名称和描述
              RELEASE_NAME="${UPSTREAM_NAME:-$TAG}"
              RELEASE_BODY="${UPSTREAM_BODY:-Automatically synced from ${{ env.UPSTREAM_REPO }} on $(date +'%Y-%m-%d %H:%M:%S')}"
              
              RELEASE_DATA='{
                "tag_name": "'$TAG'",
                "name": "'$RELEASE_NAME'",
                "body": "'$(echo "$RELEASE_BODY" | sed 's/"/\\"/g' | tr -d '\n')'",
                "draft": false,
                "prerelease": '$UPSTREAM_PRERELEASE'
              }'
              
              CREATE_RESPONSE=$(curl -s -X POST \
                -H "Authorization: token $GITHUB_TOKEN" \
                -H "Accept: application/vnd.github.v3+json" \
                -H "Content-Type: application/json" \
                "https://api.github.com/repos/$GITHUB_REPOSITORY/releases" \
                -d "$RELEASE_DATA")
              
              NEW_RELEASE_ID=$(echo "$CREATE_RESPONSE" | jq -r '.id // empty')
              
              if [ -n "$NEW_RELEASE_ID" ]; then
                echo "✅ Release创建成功，ID: $NEW_RELEASE_ID"
                
                # 上传所有Assets[5](@ref)
                UPLOAD_SUCCESS=0
                UPLOAD_TOTAL=0
                
                for ASSET_FILE in temp_assets/$TAG/*; do
                  if [ -f "$ASSET_FILE" ]; then
                    ((UPLOAD_TOTAL++))
                    ASSET_NAME=$(basename "$ASSET_FILE")
                    echo "⬆️ 上传 ($UPLOAD_TOTAL/$ASSETS_COUNT): $ASSET_NAME"
                    
                    UPLOAD_RESPONSE=$(curl -s -X POST \
                      -H "Authorization: token $GITHUB_TOKEN" \
                      -H "Accept: application/vnd.github.v3+json" \
                      -H "Content-Type: application/octet-stream" \
                      "https://uploads.github.com/repos/$GITHUB_REPOSITORY/releases/$NEW_RELEASE_ID/assets?name=$ASSET_NAME" \
                      --data-binary @"$ASSET_FILE")
                    
                    if echo "$UPLOAD_RESPONSE" | jq -e '.id' >/dev/null 2>&1; then
                      echo "✅ 上传成功: $ASSET_NAME"
                      ((UPLOAD_SUCCESS++))
                    else
                      echo "❌ 上传失败: $ASSET_NAME"
                      echo "响应: $UPLOAD_RESPONSE"
                    fi
                  fi
                done
                
                echo "📊 上传统计: $UPLOAD_SUCCESS/$UPLOAD_TOTAL 个Assets上传成功"
              else
                echo "❌ Release创建失败"
                echo "响应: $CREATE_RESPONSE"
              fi
            else
              echo "❌ 没有可用的Assets文件，跳过创建Release"
            fi
            
            # 清理临时文件
            rm -rf "temp_assets/$TAG"
            echo ""
          done

      - name: Final Cleanup
        run: |
          rm -rf temp_assets
          echo "=== 同步完成 ==="
          echo "✅ Tags 和 Releases 已同步"
          echo "📊 查看结果: https://github.com/$GITHUB_REPOSITORY/releases"
